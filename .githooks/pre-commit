#!/usr/bin/env bash
set -euo pipefail

export PATH="${HOME:-}/.local/share/mise/shims:$PATH"
REPO_ROOT=$(git rev-parse --show-toplevel)
node "$REPO_ROOT/scripts/release/cli-version.mjs" prepare
exec "$REPO_ROOT/scripts/check.sh" pre-commit
